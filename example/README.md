# Example

This example app demonstrates both supported integration styles:

- `LoadSwitch.managed` for value-driven widgets that own their internal
  loading/interaction state
- `LoadSwitch.controlled` for widgets backed by an external
  `LoadSwitchController`

Run it with:

```bash
flutter run
```
